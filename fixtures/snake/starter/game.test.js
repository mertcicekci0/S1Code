import test from 'node:test';
import assert from 'node:assert/strict';

test('replace the starter with meaningful game-rule tests', () => {
  assert.fail('Snake is not implemented. Add the requested game and tests.');
});
